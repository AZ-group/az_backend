TODO DSI 
========

- Resolver problema de PRI KEY compuestas con campos virtuales 

=> OK. No hay real problema porque las tablas tienen al menos un campo AUTOINCREMENTAL


- Meter schema de db_principal en /schemas/{nombre_db}

=> RESUELTO


- AutoJoins en respuestas JSON de ciertos endpoints (maestro-detalles)

=> RESUELTO así:

    class MaestroModel extends Model
    { 
        // ...

        function onReading(){
            $this->join('detalle');
        }
    }


- Adaptar clase usuario 


- Resolver el tema del Multitenant multi-compañia


- Ver que pasa que genera rules y relationships vacias para `tbl_categoria_persona_persona`


- Ver que pasa con las Views de base de datos (ver sp_view.sql)